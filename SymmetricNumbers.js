function calculateSymmetricNumbers(event) {
    console.log(event);
    // const currentLength = input.length;
    // let current = String(Number(input) + 1);
    // let isSymmetric = false;
    // while (isSymmetric === false) {
    //     // Logic to find symmetry when number of digits is even
    //     if (currentLength > 2 && currentLength % 2 === 0) {
    //         let firstHalf = String(current).substring(0, currentLength / 2);
    //         let secondHalf = String(current).substring(currentLength / 2, currentLength);
    //         secondHalf = secondHalf.split('').reverse().join('');
    //         if (firstHalf === secondHalf) {
    //             isSymmetric = true;
    //             console.log("Symmetrical number", current);
    //             return;
    //         } else {
    //             current++;
    //         }
    //         // When odd number of digits
    //     } else {
    //         let centerChar = String(current).charAt(currentLength / 2);
    //         let firstHalf = String(current).substring(0, currentLength / 2);
    //         let secondHalf = String(current).substring(currentLength / 2 + 1, currentLength);
    //         if (firstHalf === secondHalf) {
    //             isSymmetric = true;
    //             console.log("Symmetrical number", current);
    //             return;
    //         } else {
    //             current++;
    //         }
    //     }
    // }
}
